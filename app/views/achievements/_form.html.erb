<%= form_for(@achievement) do |f| %>
  <% if @achievement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@achievement.errors.count, "error") %> prohibited this achievement from being saved:</h2>

      <ul>
      <% @achievement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :resume_id, :value => @resume.id %>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  <div class="field">
    <%= f.label :summary %><br />
    <%= f.text_field :summary, :style => "width:30%;height:10%", :class => 'text_field'%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
